[pytest]
testpaths = motor_calculator/tests
markers =
    femm_integration: requires a real local FEMM installation; skipped when FEMM is absent so an optional external solver never fails normal regression
